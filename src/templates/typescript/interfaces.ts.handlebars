{{#each components.schemas}}

{{#if this.[$ref] ~}}
  export type {{{schemaToInterface @key}}} = {{{schemaToInterface this.[$ref]}}};
{{else}}
  export interface {{{schemaToInterface @key}}} {

  }
{{/if}}
{{/each}}
